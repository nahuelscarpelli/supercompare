# ============================================
# SuperCompare v2 — Dependencies
# ============================================

# Scraping (HTTP only — no Playwright needed!)
httpx>=0.27.0

# API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0

# Database (for auth & history — not needed for search)
asyncpg>=0.30.0
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9

# Matching (future: fuzzy product matching)
# rapidfuzz>=3.10.0

# Utilities
python-dotenv>=1.0.1
