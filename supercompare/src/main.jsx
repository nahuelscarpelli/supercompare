import React from 'react'
import ReactDOM from 'react-dom/client'
import SuperCompare from './SuperCompare'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <SuperCompare />
  </React.StrictMode>
)